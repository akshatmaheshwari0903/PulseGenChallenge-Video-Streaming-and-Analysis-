# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/pulsegen
# For MongoDB Atlas (replace with your connection string):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pulsegen

# JWT Configuration
# Generate a secure secret key (e.g., using: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# File Upload Configuration
MAX_FILE_SIZE=500000000
UPLOAD_PATH=./uploads/videos
PROCESSED_PATH=./uploads/processed

# Socket.io Configuration
# For multiple origins, separate with commas: http://localhost:5173,http://localhost:5174
# Leave empty or unset to allow all localhost ports in development
CORS_ORIGIN=http://localhost:5173,http://localhost:5174

# Content Detection API Configuration
# Option 1: Sightengine API (Recommended - Free tier available, simpler setup)
# Sign up at https://sightengine.com/ and get your API credentials
# Free tier: 1,000 requests/month
SIGHTENGINE_API_USER=your_sightengine_api_user
SIGHTENGINE_API_SECRET=your_sightengine_api_secret

# Option 2: Google Cloud Vision API (Alternative - requires service account)
# Download service account key from Google Cloud Console
# GOOGLE_CLOUD_KEYFILE=./google-cloud-key.json
# Or use environment variable:
# GOOGLE_APPLICATION_CREDENTIALS=./google-cloud-key.json
